# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/
.graph/sessions.db

# deps
node_modules
dist
dist-ssr
.DS_Store

# rust
src-tauri/target
src-tauri/gen

# tauri
src-tauri/WixTools

# ~/.config/git/ignore global tiene `Icon?` (para el `Icon\r` de Finder de
# macOS) y ese patrón sin `/` también matchea el basename `icons`, dejando
# fuera toda la carpeta src-tauri/icons/. Reincluir explícitamente:
!src-tauri/icons/
!src-tauri/icons/**

# editor
.vscode
.idea
*.log

graft/
.graph/
.github/skills
.github/copilot-instructions.md
.mcp.json
.ignore